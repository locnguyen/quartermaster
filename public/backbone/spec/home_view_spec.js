describe('The home view', function() {
    it('should have a search form to find assets for product');

    it('should list reservations with start dates that are 2 weeks or less from today');

    it('should list products with no assets available for reserving');
});